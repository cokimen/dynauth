logging:
    level:
        org:
            hibernate: error
            springframework:
                web: debug
        root: warn
server:
    port: 8900
spring:
    application:
        name: fluidauth
#    autoconfigure:
#        exclude:
#        - org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration
#        - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
#        - org.springframework.boot.autoconfigure.jdbc.DataSourceTransactionManagerAutoConfiguration
#        - org.springframework.boot.autoconfigure.data.web.SpringDataWebAutoConfiguration
    datasource:
        url : jdbc:oracle:thin:@//localhost:1521/XE
        username : USER_BIASA
        password : root01
        driver-class-name : oracle.jdbc.OracleDriver
    database-platform : org.hibernate.dialect.Oracle12cDialect
    hibernate:
        jpa:
            use-new-id-generator-mappings : false
            show-sql: true
        hikari :
            minimumIdle : 5
            maximumPoolSize : 20
            idleTimeout : 30000
            maxLifetime : 2000000
            connectionTimeout : 30000
            poolName : HikariPoolBooks
    logging :
        level :
            org :
                hibernate :
                    SQL : DEBUG
    jpa:
        show-sql: true


 ------------

 ELECT usercode, passcode, status_active FROM mst_user WHERE usercode = ?

 INSERT INTO MST_USER VALUES ('masbro', 'masbro', 1);

 UPDATE MST_USER SET passcode = '{bcrypt}$2a$10$dXJ3SW6G7P50lGmMkkmwe.20cQQubK3.HZWzG3YB1tlRy.fqvM/BG'


 CREATE TABLE mst_user (
   usercode VARCHAR2(100),
   passcode VARCHAR2(100),
   status_active NUMBER(1,0),
   PRIMARY KEY(usercode)
 )

 CREATE TABLE mst_grouprole_to_role (
     ref_grouprolecode VARCHAR(50) NOT NULL,
   authority VARCHAR(50) NOT NULL,
   FOREIGN KEY (ref_grouprolecode) REFERENCES mst_user(usercode)
 )

 DROP TABLE mst_grouprole_to_role;


 SELECT usercode, ref_rolecode FROM mst_user   LEFT JOIN mst_grouprole_to_role ON mst_user.ref_grouprolecode = mst_grouprole_to_role.ref_grouprolecode WHERE usercode = ?